import { colors } from "@/constants/colors";
import React from "react";
import { StyleSheet } from "react-native";

const indexDesigns = StyleSheet.create({
  main: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  main_text: {
    fontSize: 40,
    color: '#121212',
    padding: 4
  },
  main_sub_text: {
    fontSize: 17
  }
});

export default indexDesigns;
